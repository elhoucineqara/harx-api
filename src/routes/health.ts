import { Router, Request, Response } from 'express';
import dbConnect from '../lib/dbConnect';

const router = Router();

// TODO: Convertir manuellement la route: health/db/route.ts
// Chemin Express: /db

export default router;
